---
// Legacy route — the site is now a single-page essay. Redirect to the anchor.
const base = import.meta.env.BASE_URL.replace(/\/$/, '');
const target = `${base}/#reproducibility`;
---
<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta http-equiv="refresh" content={`0;url=${target}`} />
    <link rel="canonical" href={target} />
    <title>Redirecting to the prayoga essay…</title>
  </head>
  <body>
    <p>This page has moved. Redirecting to <a href={target}>the prayoga essay</a>.</p>
  </body>
</html>
