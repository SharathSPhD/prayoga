# HF Space source (qbz506/prayoga)

Mirror of the live Gradio Space. `metrics.json` / `findings.json` are copied from
`site/public/data/` and `agentdojo_demo.json` is generated from the real AgentDojo
banking run (`results/tier1/agentdojo_banking.json` + redacted logs). Aggregate-only,
dual-use-gated: no model weights, no refusal vectors, no harmful prompts, injection
payloads redacted.

Deploy: `huggingface_hub` upload_folder to space `qbz506/prayoga`.
