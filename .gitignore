# macOS metadata artifacts (flagged as non-content in CLAUDE.md)
.DS_Store
._*

# session / local agent state
.remember/
scratchpad/

# python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.env
.python-version

# tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
.ipynb_checkpoints/

# experiment outputs / heavy artifacts (kept out of git; released via OSF/HF, safety-gated)
# NOTE: track input prompt datasets (data/prompts/**) but ignore everything else under data/
data/*
!data/findings_registry.json
!data/prompts/
results/
checkpoints/
*.safetensors
*.pt
wandb/

# notebooks scratch outputs
notebooks/**/outputs/

# experiment run logs (stdout from container runs)
*.log
results_*.log

# LaTeX build artifacts (keep .tex, .bib, and the compiled paper.pdf)
paper/*.aux
paper/*.log
paper/*.out
paper/*.bbl
paper/*.blg
paper/sections/*.aux

# site (Astro) build + deps
node_modules/
site/dist/
site/.astro/

# agentdojo venv + logs
.venv-ad/
results/tier1/agentdojo_logs/
